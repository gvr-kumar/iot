package com.iot.utils;

import java.math.BigDecimal;

public class MemoryMgmnt {
	/*All objects are assigned same memory in a class
	 * 
	 * Heap and Stack are part of RAM.
	 * 
	 * Temporary storage- Heap & Stack
	 * 
	 * Heap- To get the trace of objects and data used in the program
	 * 
	 * 
	 * Stack- To get the trace of program instructions (lines of code (byte code))
	 */
	
	BigDecimal b = new BigDecimal(1234567890);

}
